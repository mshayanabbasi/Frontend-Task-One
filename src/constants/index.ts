const STATUS = ["active", "inactive"];

const EMAIL_VERIFIED = ["Yes", "No"];

const GENDER = ["male", "female"];

const CITIES = ["Karachi", "Lahore", "Peshawar", "Islamabad"];

const PROFESSION = [
  "Frontend Developer",
  "Full Stack Developer",
  "Mern Stack Developer",
  "Backend Developer",
];

const COUNTRY = ["Pakistan"];

const API_URL = "http://localhost:3030";

export { STATUS, EMAIL_VERIFIED, CITIES, GENDER, PROFESSION, COUNTRY, API_URL };
